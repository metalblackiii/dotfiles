# Ephemeral claude data (these stay in ~/.claude, not symlinked)
# Listed here in case they accidentally get copied
claude/.claude/projects/
claude/.claude/history.jsonl
claude/.claude/cache/
claude/.claude/debug/
claude/.claude/file-history/
claude/.claude/ide/
claude/.claude/paste-cache/
claude/.claude/plans/
claude/.claude/plugins/
claude/.claude/session-env/
claude/.claude/shell-snapshots/
claude/.claude/stats-cache.json

# Claude Code local settings (repo path differs from global gitignore pattern)
claude/.claude/settings.local.json
claude/.claude/scheduled_tasks.lock

# Research vault content (private repo, symlinked here)
docs
profiles

# Private/secrets files (never commit)
*.local
