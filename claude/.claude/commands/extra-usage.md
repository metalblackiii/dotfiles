---
command: extra-usage
description: Shadow for the built-in /extra-usage command to prevent accidental activation (fat-finger when typing /exit).
---

This command shadows the built-in `/extra-usage` to prevent accidental activation.

Tell the user:

> Caught `/extra-usage` — looks like you meant `/exit`. No usage changes were made.

Do nothing else.
