cli:
  backend: "codex"
  prompt_mode: "arg"

event_loop:
  completion_promise: "LOOP_COMPLETE"
  max_iterations: 20
  max_runtime_seconds: 7200
  idle_timeout_secs: 900
  checkpoint_interval: 5
  prompt_file: "/Users/martinburch/repos/dotfiles/docs/ralph-gbb-phase1-prompt-2026-02-24.md"

core:
  guardrails:
    - "Stay within Phase 1 scope only"
    - "Do not modify files outside neb-ms-registry"
    - "Run relevant tests before declaring completion"
    - "If scope exceeds limits, stop and propose split/remediation"

memories:
  enabled: true
  inject: auto
  budget: 2000

tasks:
  enabled: true
